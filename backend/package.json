{
  "name": "harvelogix-backend",
  "version": "1.0.0",
  "description": "HarveLogix AI - Backend Server",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "pytest tests/ -v --cov=agents --cov=core --cov-report=html",
    "test:unit": "pytest tests/test_harvest_ready_agent.py -v",
    "test:property": "pytest tests/test_agents_property_based.py -v"
  },
  "dependencies": {
    "aws-sdk": "^2.1693.0",
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
